export type LoginLayoutProps = {
    title: string,
    backgroundImg: string,
    logo: string,
    children?: React.ReactNode
}


export type HomeLayoutProps = {
    title: string,
    children: React.ReactNode
}